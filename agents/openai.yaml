interface:
  display_name: "Video to Summary"
  short_description: "Download a video, transcribe it locally, and draft a summary"
  default_prompt: "Use $video-to-summary to turn this video URL into transcript artifacts and a draft summary, then refine the summary if needed."
policy:
  allow_implicit_invocation: true
